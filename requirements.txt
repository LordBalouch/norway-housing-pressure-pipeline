pandas==2.3.3
requests==2.32.5
pyarrow==22.0.0
python-dotenv==1.2.1


python3 - <<'PY'
import importlib.metadata as md
pkgs = ["pandas", "requests", "pyarrow", "python-dotenv", "lxml"]
for p in pkgs:
    print(f"{p}=={md.version(p)}")
PY > requirements.txt

cat requirements.txt
